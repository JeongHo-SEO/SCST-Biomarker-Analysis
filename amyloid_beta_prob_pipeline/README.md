This `python` files have to be used at

```bash
cd ../"03. vaccine_naive_889_dataset"
run 02_amyloid_beta_prob_pipeline.ipynb
```
Then, the results would be held on `amyloid_beta_prob_pipeline/results/`.

Finally, you can create `index.html file` by below:
```bash
cd docs
run build_index.py
```

Then, you can upload raw SCST files and download probability with user informations.
